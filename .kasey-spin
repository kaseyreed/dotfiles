#!/usr/bin/env zsh

alias do-update='git pull && update'
alias do-update-force='git reset --hard HEAD && git pull && update'
alias do-dev-stuff='dev style && bin/tapioca dsl'

function help() {
  echo "kasey you need help"
}